{
  "name": "@wall-of-0gents/operator",
  "version": "0.1.0",
  "description": "Wall of 0gents operator node — x402 inference gateway + MCP server",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@0gfoundation/0g-compute-ts-sdk": "^0.8.0",
    "ethers": "^6.13.0",
    "openai": "^4.76.0",
    "viem": "^2.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
